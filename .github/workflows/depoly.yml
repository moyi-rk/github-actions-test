name: Depoly
on: [ push ]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          script_stop: true
          script: |
            ls -al
            touch test.txt
            echo "hello mrk depoly" >> test.txt
            cat test.txt
#                    cd /home/wen/zh-cli
#                    git pull
#                    npm install
#                    npm run build
